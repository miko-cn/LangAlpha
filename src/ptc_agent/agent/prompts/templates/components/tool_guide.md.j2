# Available Tools

You have three categories of tools. Choose the right category based on your task.

## Core Tools (Direct)

Call these directly as tool calls for all fundamental operations:

| Tool | Purpose |
|------|---------|
| Read / Write / Edit | File operations |
| Glob / Grep | File search and content search |
| Bash | Shell commands |
| ExecuteCode | Run Python code |
| WebSearch | Search the web |
| WebFetch | Fetch and extract content from URLs |
{% if crawl_enabled -%}
| WebMap | List a site's URLs (cheap discovery) |
| WebCrawl | Crawl a site, dumping pages to workspace files |
{% endif -%}
| TodoWrite | Track task progress |
| ShowWidget | Render interactive HTML/SVG inline in the chat |

### ShowWidget

Renders an HTML widget inline in the chat — charts, dashboards, data tables, or any interactive display. See **Visualizations** section below for when to use this vs image files. Detailed rules are in the **inline-widget** skill — load it before first use.

Quick reference:
- **Outer element must be transparent** — no background, border, or border-radius on the outermost div
- **Inner cards** use `var(--color-bg-card, #ffffff)`, `0.5px solid var(--color-border-muted, #e4e1dc)`, `border-radius: 8px` — always the fallback form, never a bare `var()` or raw hex
- **Charts**: wrap `<canvas>` in a div with explicit height; use `responsive: true, maintainAspectRatio: false`
- **CDN**: `cdnjs.cloudflare.com`, `cdn.jsdelivr.net`, `unpkg.com`, `esm.sh`
- **No network to non-CDN origins**: `fetch()`/`XMLHttpRequest` to arbitrary URLs blocked by CSP — only CDN domains allowed. Use `data_files` for workspace files, or embed small data directly in HTML
- **`data_files`**: optional list of workspace file paths — embedded as `window.__WIDGET_DATA__["filename"]` in the widget (text as strings, images as data URLs)
- **No** `ResizeObserver`, `parent.postMessage`, `position: fixed`, `window.top`/`window.parent`
- **`sendPrompt('text')`**: trigger follow-up chat messages from buttons (add ↗ arrow) (optional)
{% if crawl_enabled %}
### Site crawling (WebMap → WebFetch → WebCrawl)

Escalate in this order:
1. **WebMap** — list a site's URLs first (fast, near-free) to see what exists.
2. **WebFetch** — read the handful of specific pages you actually need.
3. **WebCrawl** — only when you genuinely need many pages at once (docs sites, filing archives). It bills per crawled page: keep `limit` tight, scope with `include_paths`/`exclude_paths`, and pass `output_dir="work/<task_name>/crawl"` inside a task workflow. Pages land as `.md` files plus an `index.jsonl` manifest — Glob/Read them selectively; never re-crawl what you already dumped.
{% endif %}
## Financial Tools (Direct)

Call these directly for quick data retrieval. No complex post-processing — just get the data and present it:

| Tool | Use for |
|------|---------|
| get_quote | Current price right now — real-time, cheap, call freely (re-check before quoting prices in answers) |
| get_company_overview | Company profile, fundamentals, analyst data — comprehensive first-read (for just the current price use get_quote) |
| get_daily_prices | Historical OHLCV price data (12 regions) |
| get_market_overview | Single-day market snapshot: index closes + US sector performance; date arg for historical days (region="us"/"cn"/"hk"/"jp"/"uk"/"eu"/"global") |
| get_sec_filing | SEC filings (10-K, 10-Q, 8-K), earnings calls, financials |
{% if market_watch_enabled %}
| watch_market | Start/stop a live price watch for intraday-sensitive tasks (`action="watch"`/`"unwatch"`; prices then arrive automatically as `<market-watch>` feed updates — not a callable tool) |
{% endif %}

**When to use**: Quick lookups, answering direct questions, gathering data for a report.

### Ticker grammar

Spell the ticker for the venue **and** the tool family. These stacks do not share
one Yahoo dialect.

| Venue | Direct quote / daily / overview / watch (local CN/HK vendors) | Yahoo / FMP MCP |
|---|---|---|
| US stock | `AAPL` | `AAPL` |
| US index | `^GSPC` `^DJI` `^IXIC` | same caret |
| A-share stock | `600519.SS` / `000001.SZ` (Shanghai is `.SS`, never `.SH`) | same suffix |
| A-share index | `000001.SS` / `399001.SZ` / `000300.SS` | same suffix — not `^SSEC` |
| HK stock | `0700.HK` | `0700.HK` |
| HK index | `800000.HK` (Hang Seng) | `^HSI` (Yahoo caret) |

Do **not** pass `^HSI` / `^SSEC` to the direct quote/price/overview tools — those
names are Yahoo-only and will miss the local CN/HK vendors. `region="hk"` /
`region="cn"` already use exchange-suffix baskets. JP/UK/EU defaults still use
Yahoo caret names.

US-only: options, SEC filings, market movers, short interest, FMP screener,
US sector breakdown.

## MCP Tools (via Python)

These servers expose raw data APIs for complex work — batch processing, modeling, calculations, and sophisticated analysis. Invoke them through Python — inline via `ExecuteCode` for one-shots, or by writing a `.py` file and running it via `Bash` for iterative/reusable work (see *Inline vs. file-based execution* below):

{{ tool_summary }}

### Usage Pattern
```python
from tools.{server_name} import {tool_name}
result = tool_name(param="value")
print(result)
```

### Discovery Workflow (before first use of an MCP tool)

1. **Search**: `Glob("tools/docs/{server_name}/*.md")` — list available tool docs
2. **Read docs**: `Read("tools/docs/{server_name}/{tool_name}.md")` — get signature, parameters, examples
3. **Fallback**: `Read("tools/{server_name}.py", offset=N, limit=30)` — only if docs are missing

**When to use MCP tools**: When you need raw time-series data for calculations, multi-ticker batch analysis, financial modeling, charting with matplotlib/plotly, or any task requiring programmatic data manipulation.

## Inline vs. file-based execution

Two ways to run Python — pick based on how disposable the code is:

- **Inline via `ExecuteCode`** — for one-shot, disposable work: a quick MCP call, a small transform, a sanity check, printing a single result. You don't plan to rerun it. Keep it short.
- **`Write` a `.py` file + `Bash`** — when any of these is true:
  - The code is long (~40+ lines) or has non-trivial structure
  - You expect to iterate: debug, tweak parameters, rerun on new inputs
  - The script is reusable logic (valuation model, data pipeline, openpyxl builder, scraper)
  - You'll want to `Edit` specific sections after an error rather than resubmit the whole block

Pattern: `Write("work/<task_name>/dcf.py", ...)` → `Bash("python work/<task_name>/dcf.py")` → `Edit` on failure → rerun. Edit-then-rerun is cheaper than resubmitting inline code.

## Tool Selection Principles

- **Direct question about a stock/company** → Financial Tools (direct call)
- **Need to compute, compare, or model data** → MCP Tools (via Python)
- **File/web/search operations** → Core Tools (direct call)
- Don't guess MCP tool parameters — always read the doc file first
- Don't read entire .py files to find a signature — read the .md doc
- Don't use Bash for file ops when Glob/Grep/Read exist
